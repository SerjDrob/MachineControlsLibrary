<Window
    x:Class="MachineControlsLibrary.MessageBox.AvlMessageBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MachineControlsLibrary.MessageBox"
    xmlns:lucide="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Message"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Border Width="350" Padding="15">
        <Border.Effect>
            <DropShadowEffect
                BlurRadius="10"
                Direction="320"
                Opacity="0.2"
                ShadowDepth="5"
                Color="Black" />
        </Border.Effect>
        <Border
            Background="{StaticResource RegionBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock
                    x:Name="Caption"
                    Margin="10,0"
                    Foreground="{StaticResource ThirdlyTextBrush}" />
                <StackPanel Grid.Row="1" Margin="20,0,20,10">
                    <StackPanel Orientation="Horizontal">
                        <!--<lucide:PackIconBootstrapIcons
                            x:Name="Icon"
                            Margin="0,0,10,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />-->
                        <ContentControl
                            x:Name="Icon"
                            Margin="0,0,10,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                        <TextBlock
                            x:Name="TxtMessage"
                            FontSize="14"
                            Foreground="{StaticResource PrimaryTextBrush}"
                            Text="Your message here..."
                            TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel
                        Margin="0,20,0,0"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">
                        <Button
                            x:Name="CancellButton"
                            Width="100"
                            Margin="5"
                            Click="Cancel_Click"
                            Content="Отмена"
                            IsCancel="True" />
                        <Button
                            x:Name="OkButton"
                            Width="100"
                            Margin="5"
                            Click="Ok_Click"
                            Content="Подтвердить"
                            IsDefault="True" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>