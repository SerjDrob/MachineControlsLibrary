<Window
    x:Class="MachineControlsLibrary.AvlCommonDialog.AvlCommonDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MachineControlsLibrary.AvlCommonDialog"
    xmlns:lucide="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">

    <Border Padding="15">
        <Border.Effect>
            <DropShadowEffect
                BlurRadius="10"
                Direction="320"
                Opacity="0.2"
                ShadowDepth="5"
                Color="Black" />
        </Border.Effect>
        <Border
            Padding="2"
            Background="{StaticResource RegionBrush}"
            BorderBrush="Blue"
            BorderThickness="1"
            CornerRadius="12">
            <StackPanel>
                <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        x:Name="Title"
                        Margin="20,0,20,0"
                        HorizontalAlignment="Center"
                        Style="{StaticResource TextBlockTitleBold}" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button
                            Width="22"
                            Height="22"
                            Margin="0,0,4,0"
                            Padding="0"
                            Command="local:AvlCommands.Confirm"
                            Foreground="Green">
                            <lucide:Lucide Kind="Check" />
                        </Button>
                        <Button
                            Width="22"
                            Height="22"
                            Margin="0,0,2,0"
                            Padding="0"
                            Command="local:AvlCommands.Close"
                            Foreground="Red">
                            <lucide:Lucide Kind="X" />
                        </Button>
                    </StackPanel>
                </Grid>
                <Separator Margin="2" />
                <ContentControl Margin="10" Content="{Binding}" />
            </StackPanel>
        </Border>
    </Border>
</Window>